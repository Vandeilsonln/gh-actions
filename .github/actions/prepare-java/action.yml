name: "setup java"
description: "Download code and prepare java version"
runs:
    using: "composite"
    steps:
        -   name: Setup java with maven
            uses: actions/setup-java@v3
            with:
                distribution: 'zulu'
                java-version: 17
                cache: 'maven'